Drop your resume in this folder as `resume.pdf` (exact filename).

The email service looks for `resume/resume.pdf` by default. To use a
different name or location, set `RESUME_PATH` in your `.env` file instead.
